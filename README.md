# Certificate-generator
Built a Python-based automation tool that generates personalized certificates in bulk. Accepts a CSV file with participant data and a template to auto-fill and export certificates.
